# 설정·로직·폼 재정리 계획 (B안 — 승인 후 단계 실행)

작성: 2026-06-01. 목표 = **현장 폼 ↔ 계산 로직 ↔ 설정 페이지 셋을 일치**시키고,
항목을 의미에 맞는 그룹으로 재배치. 북극성 원칙 유지(조정 쉬움 > 디폴트 > 정밀).

---

## A. 최종 설정 페이지 구조 (tier 3 단가표 부분)

| 그룹 | 항목 | 변경 |
|---|---|---|
| **강판 자재 단가** (전용 카드) | 강판 9종 (유효폭+m당→㎡당) **+ PE폼 추가비용** | PE폼 여기로 ↑ |
| **부자재 단가 (기성품)** | 용마루 / 페이샤·후레싱(신규) / 물받이 / 엔드캡 — **규격(3m 등) + 단가 → m/개당 환산** | "자재 단가" → "부자재 단가". 철거/폐기물/PE폼 빠짐. 규격 환산 추가 |
| **하지 작업 단가** | 목재 / 철재 | 그대로 |
| **단열재 단가** (신규 그룹) | EPS / XPS / PIR / 열반사 — **제품별 ㎡당** | 소모품에서 분리, 단일→4종 |
| **절곡 단가** | 절곡 단가 + 넓이 (용마루/처마/두겁/미시/프래싱/회침/눈방지턱 **+ 페이샤·후레싱 신규**) | 페이샤 넓이 추가 |
| **소모품 단가** | 스크류 대/소 / 실리콘 | 단열재 빠짐 |
| **스틸방수 단가** | 배수로 / 홈통 / 타공 / 두겁 | 그대로 |
| **노무비** (신규 그룹) | 인건비 + 식비/숙박 + **철거 ㎡당 + 덴조 건당(신규)** | 철거(인건) 여기로 ↓, 덴조 신규 |
| **장비·운송 단가** | 스카이 / 사다리 / 비계 / 운송 + **폐기물 트럭당** | 폐기물(트럭 운반) 여기로 ↓ |

추가: **단가 계산기(PriceCalculator) 제거** — 강판 카드가 폭×단가→㎡ 역할 대체.

---

## B. 페이샤 / 후레싱 (사용자 요청 1)

> "페이샤·후레싱도 대부분 절곡으로 들어감. 절곡에 하나, 부자재(기성품)에도 3m 단위로 하나.
> 용마루 기성품 같은 거랑 똑같이 취급."

- **절곡**: `bendingWidthFascia` 신규 (추정 200mm). 자동 절곡 라인에 쓸 수 있게.
  (현재 프래싱=200 이 사실상 후레싱이니 별칭/추가 정리)
- **카탈로그 부자재(finishing)**: `fascia` 기성품 항목 추가. unit "개", lengthMm 3000,
  price 추정(예: 11,000원/개 = m당 3,667). 용마루 기성품과 동일 구조.
- 기존 `scope.eave`(m당 "처마 마감")는 → **덴조로 재정의** (아래 C). 페이샤는 카탈로그/절곡으로 흡수.

## C. 처마 / 덴조 = eave 전체 시공 (사용자 요청 2)

> "처마 덴조 = eave 전체 시공, 건당 50만/100만. 자재비(후레싱·파라펫·제이채널 절곡) 계산하면 얼마,
> 나머지는 인건비."

- `scope.eave` 의미 변경: m당 페이샤 → **덴조 전체 시공 (건당)**.
- `Estimate.denjoPrice` (또는 재사용) + 개수. 라인 = 건당가 × 개수.
- **카테고리 = 노무비** (대부분 인건비). 자재(후레싱 절곡 등)는 별도 라인으로 이미 잡히거나
  사용자가 부자재에서 추가. 덴조 라인 자체는 "덴조 시공 (건당)" lump-sum.
- 설정 노무비 그룹에 `denjoPricePerUnit` (추정 기본 700,000원/건).

---

## D. 단열재 제품별 (사용자 요청)

현재: `insulationTypes` 4종 multi-select 인데 가격은 **단일** `insulationPricePerSqm` 공통.
변경: **제품별 ㎡당 단가** 4종.

- schema: `insulationPriceEps / Xps / Pir / ThermalReflect` (㎡당, VAT포함 추정)
  - EPS(스티로폼) 4,000 / XPS(아이소핑크) 11,000 / PIR(경질우레탄) 16,000 / 열반사 6,000
- 로직 `buildLineItems`: 선택된 각 종류마다 **개별 라인** (종류별 단가 × 면적).
  지금은 종류 다 합쳐 1줄·공통단가 → 종류별 줄로.
- 폼: 단열재 multi-select 유지, 각 종류 옆 단가 표시.
- 설정: "단열재 단가" 그룹 4칸.
- `insulationPricePerSqm` 은 폴백으로 유지.

---

## E. 항목 이동 요약 (어디서 → 어디로)

| 항목 | 현재 | 이동 후 |
|---|---|---|
| PE폼 부착 ㎡당 | 자재 단가 | **강판 자재 단가** |
| 철거 ㎡당 | 자재 단가 | **노무비** (인건) |
| 폐기물 트럭당 | 자재 단가 | **장비·운송** (트럭 운반) |
| 단열재 ㎡당 | 소모품 | **단열재 단가**(4종) |
| 처마 마감 m당(eavePricePerM) | 자재 단가 | **덴조 건당**으로 의미 변경 → 노무비 |
| 페이샤/후레싱 | 없음 | **절곡 넓이 + 부자재 기성품** 신규 |
| 단가 계산기 | 자재 단가 카드 위 | **제거** |

---

## F. 실행 단계 (작은 것 → 큰 것, 각 단계 빌드 게이트)

1. **계산기 제거 + 항목 이동** (PE폼↑, 철거·폐기물→노무, 섹션 rename "부자재 단가").
   DB 변경 없음(필드 그대로, FIELDS 재배치만). 가장 안전.
2. **페이샤/후레싱** — 절곡 넓이 1 + 카탈로그 기성품 1 추가. DB: bendingWidthFascia.
3. **부자재 규격+환산** — 부자재 단가 그룹의 용마루/물받이 등에 규격 입력 + m당 환산
   (강판 카드 패턴 재사용). 필요 시 규격 필드.
4. **덴조 건당** — scope.eave 의미 변경 + denjoPricePerUnit. schema + 폼 + 로직.
5. **단열재 제품별** — 4종 단가 + 종류별 라인. schema + 폼 + 로직.

각 단계 끝에 `tsc + build` **실제 실행 + 출력 확인** 후 커밋. (이전 빌드 게이트 누락 재발 방지.)

---

## Phase 6 (이번 재정리 후) — 단가 프리셋 시스템 (사용자 요청, 기록)

> "디폴트 가격 다 정해지면 ① 기본 가격으로 재설정 버튼 ② 유저가 정한 가격 1개 이상
> 저장해서 불러오기."

- **기본값으로 리셋 버튼**: 설정 상단/하단에 "기본 단가로 재설정" — 코드 DEFAULTS 로 되돌림
  (회사 정보는 보존, 단가만). 확인 다이얼로그 필수.
- **단가 프리셋 저장/불러오기**: 새 모델 `PricingPreset { id, userId, name, snapshotJson, createdAt }`.
  현재 설정을 "표준 / 겨울 비수기 / 프리미엄" 등 이름으로 N개 저장, 드롭다운으로 전환.
  설정 헤더에 프리셋 셀렉터. 적용 시 현재 단가에 덮어쓰기(저장 전까지는 미반영).
- 단가 디폴트가 충분히 안정된 뒤(천보 데이터 + 실거래가 확정) 진행 — 그래야 "기본값"이 의미 있음.
- AGENTS.md 에도 이미 "내 단가 프리셋 저장/불러오기 (deferred — post-v0)" 로 기록돼 있음.

## 미결/확인 필요
- 페이샤·후레싱·덴조·제이채널 가격: 실거래가 없으면 추정치로 넣고 사용자 조정.
- 덴조 자재(후레싱/파라펫/제이채널)를 덴조 라인에 합칠지 vs 별도 라인 유지: 별도 유지 추천
  (북극성 — 다 보이고 다 수정 가능).
